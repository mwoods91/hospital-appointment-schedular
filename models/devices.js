const sql = require("../database/index");

// constructor
const Devices = function (devices) {
  this.id = id;
  this.name = devices.name;
  this.os = devices.os;
  this.manufacturer = devices.manufacturer;
  this.model = devices.model;
  this.mac_address = mac_address;
};

module.exports = Devices;
